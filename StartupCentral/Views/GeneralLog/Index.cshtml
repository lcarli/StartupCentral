@model IEnumerable<StartupCentral.Models.GeneralLog>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutGL.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UsuarioId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Datetime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Action)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UsuarioId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Datetime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Action)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GeneralLogId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GeneralLogId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GeneralLogId })
        </td>
    </tr>
}

</table>
